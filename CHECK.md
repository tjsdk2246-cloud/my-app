# CHECK — 퇴원예고·병상회전율 관리 챗봇 점검 결과

작성일: 2026-08-28

## 전체 판정: 실패 (미통과)

로그인·보안 관련 기능은 정상이지만, PLAN.md 성공 기준 10개 중 7개가 아직 구현되지 않아 이번 사이클 목표(리포트 자동 생성)는 아직 완성되지 않았다. 버그는 아니고, PLAN.md 작업 목록 14개 중 1·2번(로그인, 업로드 API)만 끝난 정상적인 진행 상태다.

---

## 1. 성공 기준 판정 (PLAN.md 기준)

| # | 성공 기준 | 판정 | 빠진 것 |
|---|---|---|---|
| 1 | 업로드 시 진료과별·시간대별·사유별 통계 화면 표시 | ❌ 미충족 | 리포트 화면 자체가 없음 (작업 9·10 미착수) |
| 2 | 목표치(80%/60%) 대비 당월 현황 화면 표시 | ❌ 미충족 | 계산 로직·화면 없음 (작업 5·10 미착수) |
| 3 | 업로드 데이터 서버 저장 및 전월·전년 비교 | ❌ 미충족 | Supabase 연결 자체가 없음 (작업 4·6 미착수) |
| 4 | 이상 징후 판정 없이 수치 비교만 표시 | ❌ 미충족 | 비교 기능 자체가 없어 검증 대상이 없음 |
| 5 | 값 없으면 "데이터 없음" 표시 | ❌ 미충족 | 파싱 로직 자체가 없음 (작업 3 미착수) |
| 6 | 개인식별 컬럼 있으면 업로드 거부 | ✅ 충족 | — |
| 7 | 챗봇이 데이터 범위 안에서만 답변 | ❌ 미충족 | 챗봇 라우트/화면 자체가 없음 (nice to have) |
| 8 | 리포트는 화면 확인만, PDF 없음 | ❌ 미충족 | 리포트 화면이 아직 없어 검증 불가 |
| 9 | 공용 비밀번호 없이 접근 불가 | ✅ 충족 | — |
| 10 | `npm run lint`/`npm run build` 오류 없이 통과 | ✅ 충족 | — |

**3/10 충족.**

---

## 2. 설계(DESIGN.md) 대비 구현 Gap 목록

지금까지 고친 항목은 "해결됨"으로 표시했다.

| 상태 | 항목 |
|---|---|
| ✅ 해결됨 | 비밀번호 미설정 시 인증 우회 가능하던 문제 (proxy.ts 503 처리 추가) |
| ✅ 해결됨 | 로그인 세션 값이 비밀번호와 동일한 값이던 문제 (무작위 서명 토큰 방식으로 교체) |
| ✅ 해결됨 | 로그아웃 기능 없음 |
| ✅ 해결됨 | 로그인 안 된 상태로 업로드 API 호출 시 성공으로 착각하던 문제 (401로 응답) |
| ✅ 해결됨 | 로그인 비밀번호 무제한 시도 가능하던 문제 (5회 실패 시 5분 잠금) |
| ✅ 해결됨 | 로그인 오류 화면이 항상 같은 문구만 보여주던 문제 (서버 메시지 표시) |
| ✅ 해결됨 | 개인정보 검사가 첫 번째 시트만 보던 문제 (전체 시트 검사로 확대) |
| ✅ 해결됨 | 개인정보 검사가 서식 섞인 헤더(리치 텍스트)를 못 잡던 문제 |
| ⚠️ 부분 해결 | 개인정보 키워드 목록 확대(성함·차트번호·MRN 등 추가) — 그러나 여전히 "금지어 목록" 방식이라 목록에 없는 표현이나 셀 내용 속 개인정보는 못 잡음 |
| ✅ 해결됨 | 업로드된 엑셀에 필요한 컬럼(진료과·퇴원예고일시 등)이 실제로 있는지 확인하지 않던 문제 (⚠️ DESIGN.md의 가정 컬럼명 기준, 실제 EHR 파일로 재확인 필요) |
| ✅ 해결됨 | 개인정보 거부에 이어, 업로드 **성공** 시에도 실제 컬럼명을 노출하지 않도록 응답 축소 |
| ❌ 미해결 | 빈 시트(내용 없는 엑셀)를 막지 못함 |
| ❌ 미해결 | 파일 크기·확장자 제한이 없어 큰 파일이나 이상한 형식에 취약 |
| ❌ 미해결 | 요청 형식이 깨졌을 때 400이 아니라 500 오류로 떨어짐 |
| ❌ 미해결 | 같은 기준 연월 중복 업로드·동시 업로드를 막는 장치 없음 |
| ❌ 미해결 | 기준 연월이 미래이거나 파일 속 실제 날짜와 달라도 검증 안 함 |
| ❌ 미해결 | DESIGN.md는 "xlsx 라이브러리 재사용"이라 적혀 있지만 실제로는 exceljs 사용 (문서 미갱신) |
| ❌ 미해결 | 로그인 성공 후 항상 홈으로만 이동, 원래 가려던 화면으로 안 돌아감 |
| — 해당 없음 | 업로드 화면·리포트 화면·저장·비교·챗봇 (아직 착수 전 작업, 버그 아님) |

---

## 3. 보안 점검 결과 (배포 전 관점)

| 상태 | 심각도 | 항목 |
|---|---|---|
| ✅ 해결됨 | 매우 심각 | 비밀번호 환경변수가 비어 있으면 로그인 없이 우회 가능하던 문제 |
| ✅ 해결됨 | 심각 | 세션 쿠키가 비밀번호와 사실상 동일한 값이라 유출 시 비밀번호 복원 가능하던 문제 |
| ✅ 해결됨 | 심각 | 개인정보 차단 장치가 엑셀 서식·시트 구성만 바꿔도 뚫리던 문제 |
| ⏸️ 보류 (의도적) | 심각 | `.env`에 이 앱이 쓰지 않는 강력한 토큰(GITHUB_TOKEN 등)이 있음 — CLAUDE.md에 정의된 CLI 작업용이라 지금은 그대로 둠. Vercel 배포 직전 재검토 필요 |
| 미해결 | 보통 | 업로드 파일 크기·형식 제한 없음 |
| 미해결 | 보통 | 인증 검사가 proxy.ts 한 곳에만 의존 (이중 방어 없음) |
| 미해결 | 보통 | 로그인 5회 제한이 IP별이 아니라 전역이라, 제3자가 정상 사용자를 잠글 수 있음 |
| 미해결 | 보통 | 클릭재킹 등을 막는 기본 웹 보안 헤더가 하나도 없음 |
| 미해결 | 경미 | 로그인 요청 형식 검증 부족, 응답에 엑셀 내용 그대로 노출, CSRF 보완 여지 |

---

## 4. 다음에 고쳐야 할 항목 (우선순위 순)

**Vercel 배포(Part 5) 전에는 꼭**
1. 업로드 파일 크기·확장자 제한 추가.
2. `.env`의 미사용 CLI 토큰을 실제 배포 환경변수에 그대로 복사하지 않도록 정리.
3. 로그인 5회 제한을 IP별로 바꾸거나, 최소한 공용 비밀번호를 추측하기 어려운 값으로 설정.
4. 기본 웹 보안 헤더(클릭재킹 방지 등) 추가.

**여유 있을 때**
5. 요청 형식이 깨졌을 때 500 대신 400으로 안내하도록 처리.
6. 빈 시트, 중복/동시 업로드, 미래 연월 등 예외 상황 검증 추가.
7. DESIGN.md의 "xlsx" 표기를 실제 사용 중인 "exceljs"로 수정.
8. 로그인 후 원래 가려던 화면으로 돌아가도록 개선.
9. 인증 검사를 API route에도 이중으로 걸어 안전망 추가.

**아직 손대지 않은 핵심 기능 (버그 아님, PLAN.md 작업 3~12)**
- 엑셀 파싱, 데이터 저장, 목표 대비 계산, 전월·전년 비교, 시간대·사유별 집계, 업로드 화면, 리포트 화면, 질의응답 챗봇
