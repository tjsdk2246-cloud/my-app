@AGENTS.md

# 프로젝트 규칙 (초안)

## 프로젝트 개요

이 저장소는 상급종합병원 전략기획팀이 쓸 경영 분석 챗봇을 담는다.

- **경영지표 분석·보고 챗봇** — 잠시 보류. 관련 문서(PRD.md·PLAN.md·DESIGN.md)는 `trash-can/`으로 옮겨뒀다.
- [PRD.md](PRD.md) — 퇴원예고·병상회전율 관리 챗봇 (퇴원예고율·완결률, 시간대별 퇴실 통계) — **현재 진행 중인 프로젝트**

기능을 구현하기 전에 PRD.md의 "주요 기능"과 "범위와 비범위"를 먼저 확인한다.

## 작업 방식

- 모든 설명과 주석은 한국어로 작성한다.
- 새 파일은 `my-app` 폴더 안에만 만든다.
- 코드를 바꾸면 무엇을, 왜 바꿨는지 한 줄로 알려준다.
- 이미 설치된 서브에이전트는 필요할 때마다 적극 활용한다.

## 작업 절차 (검증 루프)

아래를 매번 반복한다.

1. 변경한다.
2. `npm run lint`와 `npm run build`를 실행해 오류가 없는지 확인한다.
3. 화면이 바뀐 작업이면 `npm run dev`로 띄워 실제로 보이는지 확인한다.
4. 문제가 있으면 고치고 다시 1)로 돌아간다.
5. 통과하면 무엇을, 왜 바꿨는지 한 줄로 요약한다.

## 기술 스택 (고정)

- Next.js (App Router) + TypeScript + Tailwind CSS
- 다른 프레임워크·라이브러리로 바꾸거나 마이그레이션을 제안하지 않는다. (PRD 8번 "기술 스택" 고정 사유 참고)
- 배포는 Vercel을 사용한다.

## AI가 반드시 지킬 규칙

- 업로드된 엑셀에 없는 값은 추정하지 않고 "데이터 없음"으로 표시한다. 임의로 숫자를 채우거나 추측하지 않는다.
- KPI 이상 징후 판정 기준(전월 대비 또는 전년 동월 대비 중 하나라도 **±10%**)은 경영지표 챗봇(현재 보류 중, `trash-can/PRD.md`)에서 정의한 규칙이다. 재개 전까지는 적용 대상이 없다.
- 퇴원예고·완결률 챗봇(PRD.md, 현재 진행 중)에는 이상 징후 판정을 넣지 않는다. 수치 비교만 보여준다.
- 질의응답 챗봇 기능은 업로드된 데이터 범위 안에서만 답변하고, 데이터에 없는 내용은 모른다고 답한다. 추측하지 않는다.
- 인증은 공용 비밀번호 하나로만 제한한다. 사용자별 계정·로그인·권한 관리 기능을 만들지 않는다.
- 환자 이름·등록번호 등 개인을 식별할 수 있는 정보를 화면·로그·저장 데이터 어디에도 남기지 않는다.

## 보안

- OpenAI, Supabase, Vercel, GitHub 등 외부 API 키·토큰은 `.env`에만 두고 코드에 하드코딩하지 않는다.
- `.env`, `node_modules`는 `.gitignore`에 반드시 유지한다. 절대 커밋하지 않는다.
- 이 저장소의 문서·데이터는 병원 내부 대외비다. 외부 공개 소스나 서비스에 원본 데이터를 올리지 않는다.
- 과거 데이터를 한 번에 넣을 때 쓰는 `import-data/` 폴더(실제 병원 엑셀 원본이 들어있음)도 `.gitignore`에 반드시 유지한다. 이 폴더는 로컬 전용이며, Vercel에 배포된 서버는 접근할 수 없다.
- 외부 서비스 인증이 필요하면 토큰 값을 사용자에게 묻거나 채팅에 출력하지 않고, `.env`에 있는 값을 읽어서 사용한다.
  - 예: Supabase 작업이 필요하면 Supabase CLI를 설치해 `.env`의 `SUPABASE_ACCESS_TOKEN`으로 인증한다.
  - 예: Vercel 작업(배포 등)이 필요하면 Vercel CLI를 설치해 `.env`의 `VERCEL_TOKEN`으로 인증한다.

## 파일 삭제

파일을 지워야 할 때는 바로 삭제하지 않고, `trash-can` 폴더를 만들어 그 안으로 옮겨 둔다. 작업이 끝난 뒤 사용자가 직접 확인하고 삭제한다.

## 범위 밖 (만들지 않는 것)

- EHR·EDW 등 병원 내부 시스템과의 실시간 자동 연동(API 연동)
- 경영진 보고 외 용도(임상 의사결정, 처방 지원 등)로의 활용
- 다수 병원·타 기관 데이터를 함께 다루는 기능
- 리포트 자동 이메일 발송 등 외부 시스템 연동
- 사용자별 계정 관리, 권한별 접근 제어

각 PRD의 "범위와 비범위" 항목이 이 목록보다 우선한다. 새 기능을 추가하기 전에 해당 PRD를 먼저 확인한다.
