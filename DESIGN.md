# DESIGN — 퇴원예고·병상회전율 관리 챗봇 (PLAN.md 기반)

## 1. 화면 구성 (무엇이 어디에 보이는지)

**① 로그인 화면**
- 경영지표 챗봇과 같은 공용 비밀번호 화면을 그대로 재사용한다. 즉, 두 프로젝트가 같은 비밀번호·같은 로그인 상태를 공유한다.

**② 업로드 화면**
- 엑셀 파일 1개를 선택하는 영역과 "업로드" 버튼만 있다. 기준 연월은 사람이 입력하지 않고, 엑셀 안의 "조회년월 : 2026-07" 표기에서 자동으로 읽는다.
- 업로드한 엑셀에 이름·등록번호처럼 개인을 식별할 수 있는 컬럼이 보이면, 업로드를 막고 "개인정보로 보이는 항목이 있어 업로드할 수 없습니다" 안내 문구를 보여준다.

**③ 리포트 화면**
- 위쪽에 목표 대비 카드 2개가 나온다: 퇴원예고율(현재 값 / 목표 80% 이상), 퇴원예고완결률(현재 값 / 목표 60% 이상).
- 그 아래에 진료과별 통계표(과별 퇴원예고율·완결률)를 보여준다.
- 그 아래에 시간대별 퇴실 건수를 표로 보여준다.
- 그 아래에 퇴원예고 사유별 건수를 표로 보여준다.
- 그 아래에 전월·전년 대비 비교 수치를 표로 보여준다. (이상 징후 강조 없이 숫자와 증감만 표시)
- 값을 계산할 수 없는 항목(예: 비교할 전년 데이터가 아직 없음)은 임의로 채우지 않고 "데이터 없음"으로 표시한다.
- PDF 다운로드 버튼은 없다. 화면에서만 확인한다.
- 화면 아래쪽에 챗봇에게 궁금한 점을 물어보는 대화창이 있다. (nice to have)

## 2. 데이터 흐름 (입력 → 처리 → 출력)

1. **입력**: 사용자가 엑셀 파일 1개를 업로드한다. (기준 연월은 파일 안에서 자동으로 읽는다)
2. **처리**:
   - 서버가 이름·등록번호 등 개인식별로 보이는 컬럼이 있는지 먼저 확인하고, 있으면 업로드를 거부한다.
   - 서버가 엑셀에서 진료과별 퇴원예고율·퇴원예고완결률·시간대별 퇴실 건수·퇴원예고 사유별 건수를 뽑아낸다. 정해진 항목 외의 값이나 빠진 값은 채우지 않고 "데이터 없음"으로 남긴다.
   - 이번 달 값을 기준 연월과 함께 저장소에 저장한다.
   - 목표치(80%, 60%)와 비교해 당월 현황을 계산한다.
   - 저장소에서 지난달·작년 같은 달 값을 찾아와 증감 수치를 계산한다. (이상 징후 판정은 하지 않는다. 비교할 값이 없으면 "데이터 없음"으로 남긴다)
3. **출력**: 화면에 목표 대비 현황, 진료과별·시간대별·사유별 통계, 전월·전년 비교 수치가 나온다. 챗봇에게 물으면 이 프로젝트에 업로드된 퇴원 데이터 범위 안에서만 답하고, 없으면 모른다고 답한다.

## 3. 기술 선택

경영지표 챗봇(현재 보류 중, 문서는 `trash-can/DESIGN.md`에 있음)과 같은 프로젝트 안에서 만들어지므로, 아래 도구를 그대로 함께 쓴다.

- **Next.js (App Router)** — 화면과 서버 코드를 한 프로젝트에서 같이 다룰 수 있는 기본 도구. Part 5에서 Vercel로 그대로 배포한다.
- **엑셀 읽기 라이브러리 (xlsx)** — 엑셀 파일 내용을 컴퓨터가 계산할 수 있는 표 데이터로 바꿔주는 도구. 경영지표 챗봇과 같은 라이브러리를 재사용한다.
- **데이터 저장소 (Supabase)** — 업로드한 숫자를 다음 달·다음 해 비교를 위해 보관해두는 온라인 창고. 경영지표 챗봇과 같은 Supabase 프로젝트를 쓰지만, 데이터가 섞이지 않도록 이 프로젝트 전용 테이블을 따로 만든다. 대외비 데이터이므로 서버에서만 쓰는 비공개 키(서비스 키)로 접속한다.
- **비밀번호 보호 기능** — 경영지표 챗봇에서 만든 것을 그대로 재사용한다.
- **OpenAI API (nice to have)** — 업로드된 데이터를 근거로 사용자의 질문에 답해주는 AI. `.env`에 이미 키(OPENAI_API_KEY)가 있다.

(PDF 생성 라이브러리는 이 프로젝트에는 필요 없다. 화면 확인만 지원하기 때문이다.)

## 4. 데이터 모델 (계산 방식·엑셀 컬럼 정의 — 실제 EHR 파일로 확인 완료)

| 항목 | 정의 |
|---|---|
| 퇴원예고율 | (퇴원예고일시가 있는 건수 ÷ 전체 퇴원 건수) × 100 |
| 퇴원예고완결률 | (퇴원예고가 있었고 당일입퇴원·가퇴원이 아니면서, 사망이 아니고 퇴실일시가 12시 이전이거나 정각인 건수) ÷ (당일입퇴원·가퇴원이 아닌 전체 건수) × 100. 분모는 예고 여부와 무관하다. |
| 당일입퇴원 | 입원일자와 퇴원일자가 같은 날인 경우 (완결률 분자·분모 모두 제외) |
| 가퇴원 | 완결률 분자·분모 모두 제외 (예고율에는 포함) |
| 사망 | 완결률 분자에서만 제외 (분모에는 포함) |
| 시간대 구간 | 퇴실일시 기준 1시간 단위 (0~1시, 1~2시, … 23~24시) |
| 필요 컬럼 | 진료과, 퇴원예고일시, 퇴실일시, 입원일자, 퇴원일자, 퇴실지연사유, 가퇴원여부, 퇴원결과 |
| 의사별 통계용 컬럼 (선택) | 의사명, 의사ID — 있으면 진료과별과 같은 방식으로 의사별 통계도 만든다 |
| 무시하는 컬럼 | 환자등록번호 등 개인정보로 보이는 컬럼은 있어도 읽지 않고 무시한다 (업로드를 막지 않는다) |

실제 EHR 출력 파일은 맨 위에 제목·출력일시 같은 안내 줄이 몇 줄 있고 그 아래에 진짜 헤더가 오는데, 시스템이 필수 컬럼명이 나오는 줄을 자동으로 찾는다.

기준 연월은 업로드 화면(또는 파일명)에서 지정하며, 저장소에는 `(기준 연월, 진료과 또는 의사)` 단위로 저장한다.
