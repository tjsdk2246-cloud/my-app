# PLAN — 퇴원예고·병상회전율 관리 챗봇 (PRD.md 기반)

## 이번 사이클 목표

PRD.md에 정의된 퇴원예고·병상회전율 관리 챗봇의 핵심 기능(목표 대비 당월 리포트 생성, 전월·전년 대비 비교)을 구현해 실제로 동작하는 MVP를 완성한다.

## 성공 기준

- [ ] 엑셀 파일 1개를 업로드하면 화면에 진료과별 퇴원예고율·퇴원예고완결률·시간대별 퇴실 통계·퇴원예고 사유별 통계가 표시된다.
- [ ] 목표치(퇴원예고율 80% 이상, 완결률 60% 이상) 대비 당월 현황이 화면에 표시된다.
- [ ] 업로드한 데이터가 서버에 저장되어 전월·전년 대비 비교에 쓰인다.
- [ ] 이상 징후 판정 없이 수치 비교만 보여준다.
- [ ] 값이 없는 항목(예: 비교할 전년 데이터가 아직 없음)은 추정하지 않고 "데이터 없음"으로 표시된다.
- [ ] 업로드한 엑셀에 이름·등록번호 등 개인식별로 보이는 컬럼이 있으면 업로드가 거부된다.
- [ ] 챗봇은 이 프로젝트에 업로드된 퇴원 데이터 범위 안에서만 답변하고, 없으면 모른다고 답한다.
- [ ] 리포트는 화면 확인만 가능하고 PDF 다운로드 기능은 없다.
- [ ] 공용 비밀번호 없이는 화면에 접근할 수 없다.
- [ ] `npm run lint`, `npm run build`가 오류 없이 통과한다.

## 작업 목록 (만들 순서대로)

1. 프로젝트1(`trash-can/PRD.md`, 현재 보류 중)에서 만든 공용 비밀번호 인증을 이 화면에도 재사용해 적용한다.
2. 엑셀 파일 1개와 기준 연월을 함께 입력받는 업로드 API 라우트를 만들고, 이름·등록번호 등 개인식별 컬럼이 감지되면 업로드를 거부한다.
3. 업로드된 엑셀에서 진료과별 퇴원예고율·퇴원예고완결률·시간대별 퇴실정보·퇴원예고 사유별 건수를 추출하는 파싱 함수를 만든다. 정의된 항목 외의 값이나 없는 값은 추정하지 않고 "데이터 없음"으로 표시한다.
4. 파싱한 데이터를 저장할 데이터 저장소(Supabase, 프로젝트1과 같은 프로젝트를 쓰되 테이블은 분리)에 연결하고, 기준 연월 단위로 저장·조회하는 함수를 만든다.
5. 목표치(퇴원예고율 80% 이상, 완결률 60% 이상) 대비 당월 수치를 계산하는 함수를 만든다.
6. 저장된 이전 달·이전 해 데이터를 조회해 전월·전년 동월 대비 증감 수치를 계산하는 함수를 만든다. (이상 징후 판정 없이 수치만 계산, 비교할 데이터가 없으면 "데이터 없음"으로 표시)
7. 시간대별 퇴실 건수를 집계하는 함수를 만든다.
8. 진료과별·사유별 퇴원예고 통계를 집계하는 함수를 만든다.
9. 엑셀 파일 1개와 기준 연월을 입력하는 업로드 화면을 만든다.
10. 목표 대비 당월 현황·진료과별 통계·시간대별 통계·사유별 통계·전월전년 비교를 한 화면에 보여주는 리포트 화면을 만든다. (화면 확인만, PDF 없음)
11. 업로드된 데이터를 근거로 답변하는 질의응답 챗봇 API를 만든다. 이 프로젝트에 업로드된 퇴원 데이터 범위 안에서만 답변하고, 없으면 모른다고 답한다. (nice to have)
12. 챗봇과 대화할 수 있는 화면을 만든다. (nice to have)
13. `.env`가 `.gitignore`에 등록돼 있는지 확인하고, Vercel 프로젝트에 OpenAI·Supabase 환경변수를 등록한다.
14. Vercel 배포 설정에 이 화면이 포함되는지 확인한다.
